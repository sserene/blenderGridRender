things to use

MakeCylinderGridFromXML.blend (run the blender session from terminal by navigating to directory and then "blender makeCylinderGridFromXML.blend")

related files:
MakeCylinderGridFromFile.py
· build: option+p
· make: in Blender’s 3D view, spacebar + "add gridded cylinder (from file)"
· at the terminal prompt, type in the name of (tab-delimited) text file with names of XML files, phi, and theta (respectively) on different lines (ex. listXML.txt) where theta is tilt around the z-axis and phi is rotation for the gridholes given in the XML file

CylinderParametersFromXML.txt
· where parameters get written to/read from